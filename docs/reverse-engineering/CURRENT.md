# Current rendering handoff

Updated 12 September 2026. This is the current entry point; `PLAN.md` and `reviews.jsonl` describe historical work. Follow the root AGENTS.md for the current single-agent workflow.

## Scope and next work

Latest execution13 September: combat clip/frame transitions, original296 Human/Monster animation headers and ground-facing are integrated. Native comparisons pass1792 clip cases,296 header cases,576 frame cases and864 facing cases. Corrected omitted death-entry Lib.Hits(100) draws and smoke allocation; repeated64 boss departures now consume64 prize Math draws and64 death-sound Lib draws. Empty special-map construction/180 tile lookups checked. Human appearance/vehicle flags, remaining animation memberships, height integration, source state/RNG, death bookkeeping and settlement remain open. See the animation/death-entry entry in special-combat.md; exact/recommendation modes remain unavailable.

Latest execution13 September: SkillSystem status phase is connected after fighters/before projectiles using native Skill-component membership. Supplied sleep/defense-down state is validated; sleep blocks charging while retained commands continue. Five composed cases plus640 native status and960 charging checks pass;24 autonomous regressions retain their counts. Animation behavior requests are now traced, but ChangeAnimation/resource playback remains unimplemented. Next prioritize animation clip/nextBehavior side effects, remaining status generation/passives, source state/height and RNG/death/settlement closure. See latest special-combat.md entry.

Latest execution13 September: projectile trails and area-cell effects are integrated;216 native projection and27 cell-effect comparisons pass, alongside512 trail-phase cases and20 encounter regressions. Controlled trails expire at birth tick+9 through the shared garbage phase. The post-fighter attribute drawing branch is proven unreachable for special terrain=-1 (324 terrain and28 phase checks). Remaining: source render mode/projectile rotation, fighter animation/status/height/source state, RNG startup/state, death hooks and settlement. Details: latest special-combat.md entry. No exact encounter or optimizer claim.

Current combat execution plan (13 September): [B0–B7 execution plan and blind spots](combat-sandbox.md#execution-plan). Execution started: content/build/scenario manifests, enforced research-only support, horizon censoring, labelled RNG draws/final states and native-checked healing/balloon allocation are integrated. See the latest entry in special-combat.md. Next: projectile trails/cell effects, active RNG startup/state producers, remaining source components/animation/height/status/death hooks; then ending/receipt and composed fidelity checks. B0 reachability closure and B1/B2 remain incomplete; no routine approval checkpoints.

Ghidra ready13 September: original hash-verified ELF imported in the dedicated ka-combat project, rebased from the loader default00100000 to0 with selected entry-byte checks. Six combat routines decompile successfully after native throw-wrapper no-return annotations. Local server extension adds read-only decompile_prepared_function for already-defined functions without falsely marking full analysis complete; MCP call on AddGuerrillaPrize passed.21 tools listed,1 program. Preparation/exports: tools/recovery/prepare_ghidra_combat.py and RE-evidence/20260912-combat/ghidra/preparation.json. Script accepts repeated --address values from audit.json; keep project closed while preparing. Full indexing/prototypes remain incomplete. The existing native Unicorn checks remain the behavioral verification route.

Tooling update13 September: user-authorized Ghidra integration installed. Codex MCP server ghidra uses pyghidra-mcp0.2.5, existing Ghidra12.1/Java21, stdio, dedicated project C:/Users/anisb/GhidraProjects/ka-combat. MCP initialization,20-tool listing and empty project listing verified; game import/decompilation in this project not yet performed. Local skill C:/Users/anisb/.codex/skills/ghidra-native-re/SKILL.md is validated. Reload Codex if the MCP tools are not discovered; do not open the same project in two server processes. Verification script/report: C:/Users/anisb/.codex/tools/ghidra-mcp/. Existing historical Ghidra projects were not modified.

Latest combat batch (14 September): [native save-stream byte order and the first B4 composition fixtures](special-combat.md#native-save-stream-byte-order-and-the-first-b4-composition-fixture--14-september-2026). Native execution settled the save-stream byte order (big-endian writers with inverting readers), so the earlier little-endian assumption is retracted and `combat_receipt.py` packs/unpacks big-endian. Two composition fixtures now run original slices under Unicorn in one address space and machine: 22 slices for the stream/component round trips and 31 for `Entity.AddTreasure` → result queue → `TreasureComponent` serialize/deserialize, which matches `package_receipt` field for field, records the boundary order and selector, and records a width boundary (int32 `dataId` keeps 32 bits in memory but only 16 on the stream). The B3/B4 tooling, docs and evidence from the working copy were landed into this tree; `run_combat_checks.py` reports 134/134 here. Receipt confirmation/EXP/teardown, chest-open result derivation and the `TreasureBoxResult` dispatch composition remain open, and nothing here is runtime-validated.

Latest combat batch: [sequential starting state and attack effect lifetimes](special-combat.md#sequential-starting-state-and-attack-effect-lifetimes--13-september-2026). Explicit pre-placement inputs now drive native per-team initialization; a controlled rear fighter changes Charging/Waiting with enemy old-cell location. Attack/text and projectile impact effects allocate and expire in shared phases, with64 original dispatch-argument comparisons,15 resource headers and192 constructor checks. Both20-roster regressions pass. Source-state producers, other cloned components, healing/balloon/trail effects and full animation/height remain; fallback starts are labeled approximations.

Latest combat batch: [projectile constructor and controller cleanup](special-combat.md#projectile-constructor-and-controller-cleanup--13-september-2026). Native component ordering, ordinary impact-tick garbage, arrow fade removal and occupancy removal are connected; fighter bodies survive.9 original constructor cases,64 garbage cases,160 modifier cases and20 complete-roster regressions pass. Initial opposing-team pre-placement state and remaining effects/resources are still open; no complete-fight fidelity claim.

Latest combat batch: [runnable scenario inputs and prize RNG](special-combat.md#scenario-runner-equipment-pets-battlefield-and-prize-rng--13-september-2026). Equipment/effective stats, house-pet ordering, refill, ranged normal attacks and explicit herb stock are connected. Native map width8 and empty missing-cell fallback correct the earlier supplied-cell assumption.25 native prize-selection comparisons establish a combat Math draw for every nonempty award. A10,000-tick synthetic CLI run passes; it is not a yield recommendation. [Runner usage and remaining steps](combat-sandbox.md). Next close initialization/effect/cleanup/RNG, then ending/receipt, then fidelity-qualified strategy search.

Latest combat integration: [navigation, occupancy and body flight](special-combat.md#navigation-occupancy-and-body-flight--13-september-2026). Shared movement now fills gaps/repositions overlap, uses ordered cell buckets, selects normal front opponents across columns and includes knockdown/departure flight.20 complete-roster cases/20000 ticks generate49 movement entries; gap/target/native movement regressions pass. Initial cell collections are explicitly supplied, not recovered map bounds. Next connect real effective equipment/pet inputs and remaining world/RNG/settlement lifecycle.

Latest combat integration: [projectiles and complete-roster smoke runs](special-combat.md#projectile-integration-and-complete-roster-smoke-runs--13-september-2026). All20 original encounter rosters/270 enemies now execute supported skill routes in the shared laboratory;20000 ticks generated642 projectiles.36 native launch-wiring checks and replacement-target/zero-HP-owner probe pass, alongside projectile/invoking regressions. Movement decisions, exact initial player state, effects/RNG, cleanup and settlement remain; complete rosters do not yet imply faithful full fights.

Latest combat integration: [area and reflection](special-combat.md#area-and-reflection-integration--13-september-2026). Shared controllers support line/band and nested reflection with per-cell Attack-before-Send batching.99 new native predicates distinguish area availability from HP-filtered damage. Four original-parameter Wairo subsets/12000 ticks and existing regressions pass. Projectile attacks, movement and full roster integration remain; no complete encounter prediction.

Latest combat integration: [shared autonomous enemy/healer controllers](special-combat.md#shared-autonomous-enemy-and-healer-controllers--13-september-2026). Reduced Knight/Kairobot laboratory now generates both sides' attacks and healing without scheduled incoming hits.24 cases/48000 ticks plus generated human death pass; original healer regressions pass. Only direct/heal routes and supplied stationary melee geometry are supported. Connect area/projectile/reflection, movement and full rosters next; no full encounter yield prediction yet.

Latest combat integration: [generated farmer actions](special-combat.md#generated-farmer-actions-joined-to-chest-events--13-september-2026). Empty-queue farmer slice now joins actual charging, recipe selection, invocation, MP payment, direct damage and chest events.72 seeded cases/72000 ticks pass;8 show release-time failures. Native filtering/eligibility/selection regressions pass. Enemy incoming events and positions remain supplied; replace them with enemy/healer AI before treating results as encounter predictions.

Latest combat integration: [chest causality and verdict boundary](special-combat.md#chest-causality-and-verdict-boundary--13-september-2026).72 controlled cases/18000 ticks now trace command targets, HP/state and each prize cause; paired mob-damage changes leave identical boss-release prize timing unchanged. Ending observation does not cancel prescribed releases.1296 original OnEnd traversal checks pass. Full caster AI, confirmation/teardown and receipt remain to integrate; this is not yet an autonomous encounter simulator.

Combat source update, 13 September: [Reddit and supplied Discord observations](combat-community-observations.md) distinguish reported recipes from code. In particular, low DEX affects ordinary accuracy rather than base critical chance; zero-HP boss state re-entry does not imply resurrection; mob-triggered cash-in remains unproven. Integrate per-command target/state/prize traces to distinguish these explanations. Offline native-code comparison remains the validation route; player resource spending is not a prerequisite.

### Wairo / Kairo combat â€” 12 September 2026, active research

Latest active work: [chest farming objective and benchmark](special-combat.md#chest-farming-objective-and-benchmark--13-september-2026). User objective is repeated boss-chest optimization, with deliberately low DEX, Counter/multi-hit backlog and healer support. No friends or biome bonuses. Native prize append repeatability passes32 cases;64 controlled schedules exercise Leaving/Damaging re-entry;72 native queue-construction cases preserve duplicate stored commands. Kairo Artisan and Wairo A-Champ/BT benchmarks saved. Wairo screenshot +60 plus10 icons corresponds to70 queued prizes;101 native counter cases checked. MP-herb use must be modeled. Next prioritize reward settlement/ending, backlog generation/release and healer/formation survival. No exact setup success or optimal stat thresholds claimed.

Latest hypothesis check: [multi-hit duration and overlap](special-combat.md#follow-up-multi-hit-duration-hypothesis--13-september-2026). Backlog fixture now168 cases/84000 ticks. Matched2/5/7-hit schedules show longer command lifetime can increase overlapping unfinished commands even with the same interruption count. This supports a duration contribution, not a claim that interruptions pause commands or that queue size predicts boxes.

Latest continuation: [healer, Holy Herb and backlog generation](special-combat.md#follow-up-healer-holy-herb-and-backlog-generation--13-september-2026). Holy Herb31 restores team MP via Human-only addition;248 native item/recovery checks.644 native healer selection checks confirm nearest injured ally, self exclusion, stable ties and possible dead-target obstruction.256 controlled queued-heal cases join interruption/knockdown and release-time MP/herb behavior.56 controlled backlog schedules join Counter and charging; rapid interruption can grow Counter backlog while suppressing new multi-hit charging. All20 encounter role baselines/270 enemies now include conditional invocation and monster MP costs; Extreme Kairo has16 or19 Heal Maddy bots, while Wairo Tank has16 four-hit Wairobots. Queued-heal fixture now uses recovered cost28 at supplied training123. 1230 native active-filter/index cases confirm unaffordable/defensive skills are removed before invocation-level indexing, so MP can change the effective mapping when levels differ. 100 composed boss departure cases distinguish retained commands from release eligibility: Wairo Tank/Kommander area skill35 fails its front-row gate after Leaving. No setup success/yield claimed; integrate actual invocation/MP/enemy schedules next.

Latest continuation: [weapon routes, Counter targets and settlement](special-combat.md#follow-up-weapon-routes-counter-targets-and-settlement--13-september-2026).1152 composed route cases distinguish direct stored skills from HP-filtered projectile/cell impacts; bow/gun Counter and ordinary multi-hit remain direct. Native Counter targets its incoming attacker.144 controlled mixed queues feed boss re-entry. Special Finish releases every supplied treasure on victory, without deduplication/clamp through256 entries (24 native prefix +7 filter cases); downstream inventory receipt still unresolved. No yield ceiling or optimal weapon claimed.

Earlier continuation: [enemy invocation initialization and constructor baselines](special-combat.md#follow-up-enemy-invocation-initialization-and-constructor-baselines).20 constructor baselines now compose270 enemies with original stats, skills and formation; examples explicitly use defeatCount0. Enemy invocation level1 means18% default activation.6528 combined native invocation checks,192 full no-reaction Attack cases,343 invoking-counter cases and28 ordered fighter-phase cases pass. Continue integrating the headless phases and tracing death bookkeeping/RNG; no complete simulator or original replay is claimed.

Previous continuation: [projectile flight and next-state decisions](special-combat.md#follow-up-projectile-flight-and-next-state-decisions):72 native launch cases,1016 trajectory updates,544 arc calculations,4608 state-decision,960 charging,480 release,292 targeting/front-row and324 terrain cases pass. Impact messages are batched after all projectile motion updates. Damage reaction writes offsetZ, not actual Z. Continue composing state transitions and tracing effect RNG.

Previous continuation: [enemy constructors and friend weakening correction](special-combat.md#follow-up-enemy-constructors-and-friend-weakening-correction). Fresh battle clone destination has no system listeners yet. Constructor level forwards directly to stat curves; raw level is not clamped to maxLevel. Native friend tests exposed/corrected unbounded Parameter.maxValue returning extraMax, not INT_MAX;1005 cases checked.

Previous continuation: [movement and ordered skill selection](special-combat.md#follow-up-movement-and-ordered-skill-selection): fixed battle movement speed, two-waypoint queue, deferred position/cell stages, resurrection/recovery/attack priority and filtered invocation index traced. Added2007 native movement and527 selection checks.

Previous continuation: [recovery, knockdown and battle termination](special-combat.md#follow-up-recovery-knockdown-and-battle-termination). Defeat requires every fighter in Leaving(8); human knockdown transitions at frame101. Resurrection placement counts living units per column after HP restoration. Added1116 isolated native timing/predicate checks. Special leader death also selects a reward before combat has necessarily ended.

Previous findings:  [battle clone serialization and starting-state preservation](special-combat.md#follow-up-battle-clone-serialization-and-starting-state-preservation). Battle clones serialize components into fresh instances. Parameters retain raw/current/max/extra fields and training levels; skill invocation lists and remote equipment levels are copied. Special-fight HP/MP refill is traced. Friend values above29999 trigger weakening rather than exclusion; staying adventurers match flag1 and stored town ID. Native System.Random is ported and tested, while active global selector/seed configuration remains unvalidated.

Evidence includes315 unique focused native slices. Existing combat checks pass; added1000 original parameter serialization round trips,50 System.Random seed initializations and12800 draws with complete-state comparisons. Original tables cover20 encounters/250 followers/27 monsters,313 equipment and136 skills. Previous findings include formation/pets, damage/hit/critical/RNG, weapon/AoE geometry, interruption/command timing, statuses/defensive reactions and logical system order. No original-game replay or complete simulator is claimed.

Next: integrate the recovered movement/selection/recovery state machine; trace remaining source-world mutations, effect/projectile RNG and tick ordering. Destination clone listeners and friend weakening are now traced/tested. User explicitly requires continuing whenever a needed next step is known; do not treat passing a batch as a reason to stop or ask approval. Follow existing [combat research](special-combat.md), preserving evidence limits.

### Surround effects â€” native research, 12 September 2026

See [surround-effects.md](surround-effects.md). Radius 3 with source rectangle excluded, source representative-cell/area-merge rules, target immunity/eligibility, shared-level interpolation, additive category/type caches, and four active consumer formulas are traced. `tools/recovery/recover_surround_effects.py` exports workspace `placement/surround-effects/catalog.{json,md}`: 81 source types, 85 effects, all 243 original Facility rows cross-checked, 498 extraction/formula assertions. Category 2 (capacity) has no active table entries and no direct GetAroundEffectValue consumer found. Builder simulation is not enabled: compound per-cell/cache identity and full baseline/level inputs must be resolved before claiming layout-wide results. This is the current research track; preserve existing builder and saved layouts.

### World builder â€” 12 September 2026

Collision preview / finite supply: [details](builder-collisions-and-limits.md). User confirmed apparent overlap was an unplaced ghost. Invalid preview sprites are hidden, valid previews labelled, and normal-source Chaos Stone supply is limited to7 (six Survey rewards plus Area94 reward). All235 facility footprints cross-checked against original MapChip sizes; 17,511 assertions and production build pass. Existing over-limit saves remain editable.


Pets/dungeons: Monster Room157 and Monster Stable162 now have one planned occupant slot, searchable original-body sprite picker, replace/remove controls, static anchored occupant markers, and local-save/undo persistence. Facility_lookup.capacity is 1 for each (types83/84); MonsterSystem.RestoreMonsterEntity `0x15bdc90` creates an ally at the room position and assigns it through FacilityComponent.AssignStaff at `0x15be00c`. Marker bottom-center positioning is editor presentation, not pet AI/animation reconstruction; the species picker is a planning catalog, not a claim that all species are hatchable. Ten dungeon appearances (MapChip categories34/89/90) are exported from native OPT/SEB with 2Ã—2 footprints and original offsets into a separate Dungeons tab. Cave numbers are appearance labels. Dungeon placement on unoccupied land outside town coverage is user-requested editor policy, not native spawning simulation. Existing Legendary Cave remains fixed. Browser check on isolated127.0.0.1 save: cave/stable placement, Diamondoom selection and visible occupant; deterministic suite 10,874 passes and production build passes, with the existing48 unrelated typecheck errors.

Surround coverage/Inn beds: generated builder assets now carry original effect tuples. `surroundCoverage` exposes radius-three spatial coverage (Town Hall uses its native core); page toggle shows selected/preview facility coverage in purple, without claiming target eligibility or simulated results. User confirms Inn Guest Beds are removable: both initial Guest Beds are now editable, including old saves; Guest Bed is no longer misclassified as a fixed workshop or shop shelf. Friend/Double/Guest replacements share extra-bed capacity, while the resident bed remains fixed. All four Inn sizes, replacements, save migration and overlay bounds are checked: 10,603 builder assertions pass; production build passes; same 48 unrelated typecheck errors.

Gate/wall ordering and map toolbar: Gate now exports separate ground and door commands per cell/facing instead of flattening the two-cell assembly. `CreateEntranceDoor 0x14766bc` sets extraDepth 26 for directions 1/2, 5 for 0/3; the renderer now preserves these and draws ground before walls. All four rotations have deterministic ordering/footprint checks (10,534 total assertions). Build/Remove and draft Rotate/Cancel now sit inside the map at the right with 56px mobile targets (64px desktop); gate draft rotation was checked through the browser UI. Production build passes; the same 48 unrelated typecheck errors remain.

Expansion spacing: `placement/builder-expansion-spacing.json` traces CheckPlace `0x15087a4..0x1508814` through ContainsExpansionChip and predicate `0x16024e8`. Existing non-hall expansion rectangles exclude new expansion footprint cells across types. Radius is existing Facility.validRange (Torch 7, Nighttime Meeting Place 8, Low Watchtower 10, Turret 12), expanding its footprint; diagonal corners count. Town Halls are excluded as spacing sources. Shared validation now enforces this for placing/moving expansions and drives dimming, preserving existing saves. This is not sum-of-radii spacing or a ban on all coverage overlap. Suite passes 10,510 assertions. Builder instant-placement policy does not model native construction/fog-state checks.

Wall adjacency correction: `placement/builder-wall-rules.json` records FencePlaceSystem.OnPlaceMapChips `0x1580618`, Direction offsets and CreateBarrier. All native type-22 barriers connect across materials using a four-cardinal-neighbor mask directly as `barrier_00.seb` frame. Original frames contain a post and positive-axis sides, with negative sides supplied by neighbors. Builder exports all 16 frames for five materials and recomputes them after placement/move/removal and in previews; gates and plot fences are excluded. Native anchors and extraDepth 5 are preserved. The user's existing saved wall line was visually checked in-browser without editing it. Deterministic suite now passes 10,378 assertions; production build passes. Static native evidence and website visual verification, not original-game runtime validation.

Indoor correction: `builder-indoor-anchor-rules.json` records the renewed native trace through PlaceChip, CreateFurniture, GetHeight and ToScreenPos. Saved furniture already used the confirmed support height; the unparented preview did not. Preview, selected diamonds and floor-cell picking now use that support consistently, with native facing-dependent depth offsets. Plot rotation now turns fixed fixtures with their plot. Built-in workbench/register/storage facility types are excluded from manual selection using recovered House layouts and native register/workbench/storage categories (55/56/57), including Cash Register. Menu-only PNGs are alpha-trimmed and visually checked; world sprite offsets/scales remain unchanged. Browser menu check confirms enlarged Torch; contact sheet covers Nighttime Meeting Place, Monster Feed and small indoor pieces.

Build/Remove use enlarged original icon-only buttons with accessible names. Builder camera zoom has one owner: native nonpassive wheel input anchors at the cursor, +/- requests multiply the live camera zoom around viewport center, and focus requests run once rather than resetting on zoom changes. `src/lib/map-camera.ts` owns anchor-preserving zoom arithmetic.

Town level editing accepts temporary empty text and commits on blur/Enter with a 1â€“100 cap. Mouse drag builds edge-connected lines for roads, native MapChip33 gravel paths, fences and walls; invalid cells are skipped and each stroke is one undo action. Shift-drag and touch drag preserve panning. This interaction is editor policy.

The separate `/world-builder` page is implemented locally in `src/pages/world-builder.tsx`; `/world-map-v2` remains a reference. Shared editor rules and drawing live in `src/lib/world-builder.ts` and `world-builder-render.ts`. The map renderer exposes optional editor drawing, terrain and sprite-hit hooks, with drag suppression and responsive resizing. Local saves use `ka-world-builder-v1`; there is no server persistence.

Implemented: four sprite-menu tabs with land inventory under Env.; S/M/L/XL assignment and individual initial furniture; full-footprint town/land/collision checks; indoor capacity and shelf-type checks; protected fixtures, Legendary Cave and ports; move/rotate/remove; reclamation and reversal; small storage fullness; town rank and expansion facilities; level overlay; undo/redo. Original map facilities are move-only and excluded from the build catalog; ports and Legendary Cave remain fixed. Every cell must be eligible, including after a move or rotation. Coverage reductions that would strand placed buildings are rejected.

Evidence: `RE-evidence/20260911-building/placement/builder-territory-rules.json`, MapChip58's 2Ã—2 core inside its 4Ã—4 reserved ring, original Facility flag bit 1 (town-area expansion), shared House capacities and recovered `plot_layout.py`. Info Board's range is not expansion. Expansion chains rooted in towns are an editor policy preventing disconnected/self-supporting coverage; do not call this a recovered exclusive town-ownership algorithm. The builder rank cap is 100, supplied by the user. Unassigned dirt parcels are an editor placeholder using original parts. Generated anchors, original facing layers, 100 plot shells and storage states are exported by `tools/recovery/export_builder_assets.py` to `builder-assets.json` and `public/world-assets/builder`; original images/catalog exports remain untouched.

Verification: `node scripts/check-world-builder.cjs` (from the app) passes 10,052 assertions, including every footprint square, expansion connectivity, 100 furnished plots Ã— four rotations, fixture protection, overlap/capacity checks, reclamation and save roundtrips. Original sprite source bounds and paths are checked. Production build passes. Browser checks cover placement, furnished plot rotation, protected workbench/removable shelves, storage fullness, save reload, remove-mode drag safety and phone-sized sprite menus. No original-game runtime validation is claimed. App typecheck still reports unrelated errors in equipment, runtime-world-render-lab and timed-events.

Correction after user report (12 September): the initial exporter confused `sizeWidth/sizeHeight` component geometry with `unitWidth/unitHeight` build geometry. Small storage and farms are 2Ã—2, not 1Ã—1; Gate reserves two cells and uses both entrance frames. Native `PlaceChip` 0x15045f0..4630 reads unit defaults when overrides are -1; 0x15048b0..48cc multiplies size by units. See `builder-unit-placement.json` and `polish-15042e0.asm` in placement evidence. Storage exports now use four original floor cells, perimeter-only walls and per-cell scalar fullness layers. Item, Treasure and Egg contents are NOT material piles: their empty assemblies render, but their gatherable contents appearance is pending and explicitly shown as unavailable. The initial generic fullness mapping for these list warehouses was incorrect.

The invisible newly placed objects came from an image-load effect tied to changing draw lists: cancelled effects could populate the cache without invalidating the canvas. `builder-sprite-cache.ts` now deduplicates asset loads and notifies the current subscriber on every completion, with visible errors/retry. Original build/remove PNGs replace generic toolbar vectors. Save geometry revision 2 backs up the old local save under `ka-world-builder-v1-before-assembly-fix`, restores accidentally deleted native buildings, and preserves placed coordinates. Expanded old footprints can require manual moves; no user objects are discarded. Browser verification after correction: the user's saved town and formerly missing objects render, Equipment Exchange has Move only, separate-origin fresh Town Hall/2Ã—2 Wood Storehouse placement and fullness render. These are website checks, not original-game runtime validation.

Previous treasure request: recover Treasure lookup and improve Item & Skill Sources / Monster Loot. Active findings, original evidence, verification and remaining callers are in [treasure-lookup.md](treasure-lookup.md). The source page now uses item/skill â†’ containing box â†’ acquisition source, with 1,295 APK-checked rows, 41 original box/sack images, terrain/soil and monster sources, and native Kairo/Wairo reward-group links. Tuesday Extreme uses original group1203 (boxes710/711); each row has 50% selection, separate from contents odds. See the treasure handoff for new leader/victory/weekday traces and remaining legacy special-boss consumers. User scope: item search and sources with large item/box PNGs and actual map monster sprites; exclude inventory-destination features. All 169 original monster body poses are exported and used on Item Sources. Treasure-system recovery is not complete.

The user accepted the latest water appearance and wants to move to a **separate world-builder page** after this cleanup. `/world-map-v2` remains a visual reference map. Do not add town ownership or editing to it.

The builder should reuse recovered rendering/assembly modules and support townhalls, facilities and roads. Townhall level affects territory size; the user states a maximum of five townhalls. Treat that cap as user-supplied gameplay information until independently checked. Check existing rule implementations/evidence before choosing territory formulas; the map's visual acceptance does not validate builder placement rules.

 Beta fixes requested by the user (17 September): removing walls/fences/paths now works like building, so a mouse drag across the map clears the same edge-connected line `placeLine` would fill, while taps, fixed structures, landmarks and other facilities are untouched (`lineTargets`/`removeLine`, gated by the new `editorLineAllowed` map hook so a drag on empty ground still pans). Store shelves are no longer restricted to the plot's own shelf type: any shelf appearance fits any plot that has a native shelf slot, and only that plot's `maxStoreShelfPlacementNums` still applies (`indoorLimit`). The page gained build tabs (`ka-world-builder-builds-v1`, up to three builds with independent state, undo history and save; the pre-tabs save is kept under `ka-world-builder-v1-before-build-tabs` and still mirrored to `ka-world-builder-v1`), a Reset builder button, a Delete build button, a full-screen mode that keeps only the tool row above a full-height map (dialogs are portalled into the full-screen root, and browsers without the Fullscreen API fall back to a fixed overlay), a Boundary button that hides the yellow town build boundary while no building is being placed, failure reasons in red with a short-lived popup over the map, and a boundary that follows a Town Hall while it is being moved (`previewTerritory`). `scripts/check-world-builder.cjs` passes 17,535 assertions (dragged removal, shelf policy, moving-hall territory, native depth ordering) and the production build passes; the 48 pre-existing unrelated typecheck errors are unchanged. These are website-editor policies and browser checks, not original-game runtime validation; removing the old shelf-type restriction is a user instruction, not a recovered native rule.

Indoor depth correction (17 September, after the user reported the 2×2 Skill Workbench painting over a shelf in front of it while the shelf behind it looked right): the builder sorted sprites by `100*(x+y)` with the multi-cell anchor added into the sum, so a 2×2 workbench anchored on its south-east cell collided with the shelf one row in front of it and the 5/20 facing offset decided the order. It now uses the recovered native cell-major key `100*(y*160+x) + 24*(x+y) + height + facing offset + layer` (`nativeDepth`, `groundDepth`, `pieceOffset` in `src/lib/world-builder.ts`, applied by `world-builder-render.ts` to facilities, plot/gate preview pieces, roads and pet markers). Everything exported by the earlier plot and gate previews is re-keyed through its sub-cell offset, so the per-piece ordering inside the shells is preserved while furniture, walls, doors and ground now compare on one axis. Multi-cell assemblies keep their native south-east anchor and no longer double-count it. Still a preview painter order, not a GPU depth-pass replay: the native renderer also splits 2×2 furniture into four 24px strips with their own offsets (`workshop-strip-tests.json`), which this single-sprite draw does not reproduce. Evidence: `placement/workshop-overlap-tests.json`, `placement/native-depth-sort-tests.json`, `placement/depth-15d6480.asm`, `placement/builder-map-size.asm`.

Gate and wall junctions (17 September, after the user asked how gates are supposed to work with walls): a Gate (MapChip103 / facility28) is a two-cell entrance whose long axis runs *along* the wall with its facing set by rotation (`Direction.OFFSETS` `0x145f4c4`); walls join only other MapChip type-22 chips (`FencePlaceSystem.OnPlaceMapChips` `0x1580618`, `builder-wall-rules.json`), so a wall keeps its cut end beside a gate and a wall cell with gates on both sides draws only its post; the door uses resource21 `sebId=4+direction` with extraDepth 26 for directions1/2 and 5 for0/3 (`World.CreateEntranceDoor` `0x14766bc`, `plot-floor-door-rules.json`); directions2/3 are horizontally mirrored in the SEB records; and each cell draws one door piece whose frame is chosen by sampling the cell one step along the wall axis — another entrance (type28) selects the continuation frame 0, anything else the end frame 1 (`entranceTexture`, `door-layer-mirror-rules.json`). The export already carried both frames per direction but assigned them from the gate's own two-cell footprint, so two gates placed end to end repeated an end post with two lamps between them; `world-builder-render.ts` now samples the live neighbouring cells instead (lone gates are unchanged) and `check-world-builder.cjs` covers lone, walled and chained gates (17,542 assertions). Still a preview painter, not a GPU depth replay: the gate art is the exported wooden door set textured through `Wall[facility[28][28]]`, and plot entrances keep their layout-chosen frames.

Gate rotation limit (17 September, user-confirmed in-game observation, corrected the same day): a Gate only turns **90 degrees**, so its two legal orientations are the ones that keep the door on its own cells with the hinge on the same side — facing east (direction1) and facing south (direction2). The 180-degree flips (0 and 3) are forbidden: their exported artwork is offset half a cell off the footprint, which is what made one of the user's two gates look wrong. Rendering all four directions against a wall confirmed that only 1 and 2 sit centered on the cells. `allowedDirections`/`rotatedDirections`/`legalGateDirection` in `src/lib/world-builder.ts` clamp Gate creation and rotation to 1/2 (`makeItem` starts a gate facing east, `rotateItem` cycles 1↔2 and snaps a forbidden facing onto its legal partner *on the same axis* so no cell moves), `decodeWorld` normalizes legacy saves the same way, and the build menu lists the draft's real 1×2 slot instead of the raw asset 2×1. `check-world-builder.cjs` asserts the 1↔2 cycle, the direction set, the 90-degree footprint swap, the legacy save snap and that other facilities still rotate through all four directions (17,551 assertions). This is a user-supplied gameplay rule, not an independently recovered native flag.

## Plot assembly correction

The first standalone Skill Shop preview was rejected: incomplete door, incorrect wall lookup, grass perimeter, and missing entrance fence transitions. Corrected standalone stages are `RE-evidence/20260911-building/placement/skill-shop-stage-0..3.png`, generated by `render_skill_shop_plot.py` (argument 0..3). The user accepted the corrected Skill Shop image. Static house-card preview integration is complete; broader lifecycle validation remains pending.

Native corrections: MapChip43 relatedDataId54 points to Facility54, whose wall6 selects wall image43 (`wall_06.png`), via `get_wallData` 0x162d538. PlaceLand selects category0 dirt at 0x15a2558. Entrance cells choose adjoining frames at 0x1610fb4; OnPlaceMapChips changes neighboring type16 fences to post layer4 at 0x1580a00..0x1580a34. Two-cell entrances and successive chosen edge positions are checked against the user's original-game build screenshots. Bounds and logical fence occupancy pass for all four scenes; painter ordering and full construction/removal lifecycle remain outside this standalone test.

Material mappings for all 25 original House rows are extracted by `recover_plot_materials.py` into `plot-materials.json`. `render_plot.py <stage> <houseId> <sizeIndex>` applies these to S/M/L/XL shells; stage1 was checked for all 100 combinations. Overview: `plot-previews/all-house-shells.png`. These are material previews, not proof that every table entry is selectable at every size. All 25 previews now include initial furniture selected by HouseData, using SetupHouse 0x15a683c formulas in plot_layout.py. Missing (-1) slots are omitted; one-cell and two-cell pieces use their original dimensions and directions. All 100 size/type combinations pass containment, no-overlap, and sprite bounds checks; generalized formulas agree with the four earlier Skill Shop layouts. Monster House floor support is 8; others are 10. Overview: plot-previews/all-house-interiors.png. The user accepted the furnished S overview; larger sizes have deterministic checks but no direct user visual confirmation. Fence sprites include rail, hedge, and flowering-shrub variants; perimeter dirt and entrance paving are shared.

## Current map implementation

App: `artifacts/kingdom-adventures`. Main visual-map renderer: `src/pages/runtime-world-render-test.tsx`. Recovered map facility and ground data: `src/game-data/native-map-facilities.json` and `native-map-ground.json`. Original inputs and targeted native disassembly: workspace `RE-evidence/G2.1/39257e72291d` and `RE-evidence/20260911-building/placement`.

This map includes the recent facility visibility/placement, ground and port corrections. User feedback accepted multiple compound assemblies and direction 0. Those confirmations are visual checks, not proof that every facility, rotation, state or placement rule is complete. A previously noted extra bridge-fence piece was explicitly deferred. Broad TypeScript checking still reports pre-existing errors; do not describe the repository as passing all checks.

## Water: what is recovered and what is a website choice

- Original assets: `public/world-assets/chip/mizu00..03.{png,opt}`, `mizu_edge.{png,opt}`, and `world-assets/game_2/{back,effect_00}.png`. Keep these source images unchanged.
- Native shoreline selection and water-distance rules: `src/lib/native-water.ts`, `src/game-data/native-water.json`. Source functions include MapSystem.GetWaterEdgeIndex `0x15b25ac` and UpdateAroundWaterDarkness `0x15b4394`. The renderer applies nearest-land distances to the displayed terrain; this is not a replay of every historical terrain mutation.
- Ocean scroll: `src/lib/native-ocean.ts` and `src/components/ka/native-ocean-background.tsx`, from GameForm.DynamicRender `0x16bee54`. Normal target is 20 ticks/second; the horizontal cycle is 480 ticks. No camera-follow ocean offset was established. The water texture itself moves with the map.
- The original `effect_00` viewport lighting overlay is static, not a time-of-day simulation. The user explicitly excluded time-of-day tint.
- Water PNG nontransparent pixels have alpha 128. Image._getAlphaType `0x2330588` and ShaderBuilder.GetNameId `0x22df120` lead to ordinary transparent texture blending. Shader program text multiplies texture by vertex color. Read serialized `m_State.rtBlend0` for blend factors: UnityPy's text shader export omits them. Ordinary source-over blending does **not** explain the remaining original-game perimeter difference.
- **Website solution, explicitly requested by the user:** `src/lib/water-surface.ts` assembles a continuous 48x24 repeat from the original deep-water tile. It fills the viewport beneath land, paints the shallower water colors and applies the original uniform opacity once. This avoids the sharp visual perimeter and tiny gaps caused by separately scaled translucent diamonds. It adds no map cells or eligible placement area. Do not describe this infinite surface as a recovered native rule.

The seam/dots were absent in the checked browser view after dragging, and the user accepted the result. Exact native perimeter compositing remains unresolved; do not reopen that research solely for visual parity. The renderer currently uses water frame 0; the original two-frame water animation is not implemented.

## Focused verification

From the workspace root:

```powershell
node RE-evidence/20260911-building/placement/check_water.cjs
node RE-evidence/20260911-building/placement/check_ocean.cjs
.venv/Scripts/python.exe -W ignore::DeprecationWarning RE-evidence/20260911-building/placement/check_water_surface.py
```

These cover 256 shoreline masks plus distance/boundary cases, 4,800 arithmetic comparisons, and eight original water asset/frame combinations for exact repeat coverage and compositing. They are deterministic asset/formula tests, **not** 4,800 comparisons against game screenshots or runtime output. Original-game instrumentation is still unavailable.

House-card integration: `src/components/ka/plot-preview.tsx` uses the existing Building IDs and PlotSize definitions, with generated native-resolution images in `public/world-assets/plots`. Regenerate exports using `publish_plot_previews.py`. Browser checks passed for Sâ†’XL and grouped shop selection. Typecheck still reports unrelated existing errors; none in houses.tsx or plot-preview.tsx. This is a static reference preview, not the future editable builder renderer.


Combat collection follow-up: native HashSet slot reuse/order now has 4,016 differential operation/enumeration checks and two capacity rebuild checks (`collection-checks.json`). A new entity can occupy an earlier freed slot. Subset lifecycle tracing also found matching component changes send OnEntityAdded even for existing members; initial subset population does not. Continue with native callback checks and dictionary-to-subset initialization order; see special-combat.md. Full battle replay remains unvalidated.

Combat integration follow-up: 4,012 native dictionary checks, 24 subset callback checks, 32 four-projectile phase checks, 160 resumed skill-command states and36 full dispatcher/skill-queue fixtures now pass. Research models preserve freed-slot order, trajectory-before-impact batching and same-call command draining. Remaining work is explicit in state.json; no complete simulation or original runtime replay claimed.

Combat initialization correction: formation order is temporary; tick updates retain original roster order. Three native two-team initialization fixtures confirm that each team finishes reset/initial decisions before the next initializes. All20 enemy baselines now preserve original fighter order and expose formationOrder separately. Further native checks cover192 defender reactions,64 attacker activations,384 damage wrappers,75 raw parameter subtractions,86 waiting/using-skill cases,100,005 interval outer-arithmetic cases (host pow supplied),640 status states,64 attack-effect branches,96 modifier lifecycles and256 modifier clone roundtrips. Animation modifier completion can destroy an entity when its stored destroy flag is true; ordinary hit-shake sets it false. Continue with shared-state phase integration and source-clone lifecycle; no complete fight replay claimed.

Combat integration continuation: initialization now has a portable per-team mutation pass compared against native callback order. Combined Attack64 and nested reflection64 checks join both units' SkillComponents; reflection84 checks join forced-hit payload to native HP subtraction. Lethal reflection does not abort the outer Attack tail. See special-combat.md and the new combined/nested-attack and reflection-event reports. Remaining work includes state-entry/command interruption integration and whole-world ordering; exact runtime replay is still unvalidated.

Interruption integration:384 original HP/state/skill-command compositions and384 original FighterSystem.Update/AISystem compositions now check retained command progress through lethal and zero-damage interruptions. Skill effects remain explicit supplied calls in these fixtures; continuing with remaining effect branches.

Combat continuation: joined status application96, skill dispatcher416 (52 combat rows), sound gate272, multicast/reentrant dispatch18, initialization registration2 and attack subscriber batch192 native checks pass. Native special-world ordering places Battle's entire effect batch before Fighter's HP batch; per-cell/projectile skill sound consumes Lib RNG. Exact whole-world replay remains unvalidated; next join effect/entity lifecycle with this dispatch order and complete the mutable world simulator.
