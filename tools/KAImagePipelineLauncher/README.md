# KA Image Pipeline Launcher

This small Windows launcher starts the local API server, starts the Kingdom Adventures frontend, then opens the browser.

Default browser target:

```text
http://localhost:5173/equipment
```

To open a different route, pass it as the first argument:

```bat
"Start KA Image Pipeline.exe" /jobs
```
